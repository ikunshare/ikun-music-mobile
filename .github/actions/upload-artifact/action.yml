name: Setup
description: Setup Env

runs:
  using: composite
  steps:
    - name: Upload Release APK Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ikun-music-mobile-apks
        path: android/app/build/outputs/apk/release/*.apk
        if-no-files-found: error
